# gritlab-hackathon
Sports Betting Simulator

## How to run.

Go into each directory and mvn clean spring-boot:run

```
cd api-gateway
mvn clean spring-boot:run
```

```
cd player-service
mvn clean spring-boot:run
```

```
cd betting-service
mvn clean spring-boot:run
```